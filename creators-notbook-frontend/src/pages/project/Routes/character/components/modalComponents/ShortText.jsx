import { Box } from "@mui/material";
import { string } from "prop-types";

ShortText.propTypes={
  name:string,
  value:string
}
export default function ShortText({name, value}){
  return (
    <Box>
      
    </Box>
  )
}